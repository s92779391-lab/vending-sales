services:
  - type: web
    name: vending-sales
    runtime: node
    region: singapore
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: vending-db
          property: connectionString
      - key: NODE_ENV
        value: production
